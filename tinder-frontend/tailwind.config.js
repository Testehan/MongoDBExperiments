/** @type {import('tailwindcss').Config} */
export default {
  content: ["./src/**/*.{js,jsx,ts,tsx}", "./index.html"], // files where tailwind is used..sure we don't need all those extensions, but just added them :)
  theme: {
    extend: {},
  },
  plugins: [],
}

